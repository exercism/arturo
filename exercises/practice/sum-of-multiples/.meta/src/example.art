sumOfMultiples: function [factors limit][
    multiples: []
    factors: select filter factors => zero? => [& < limit]

    loop factors 'factor ->
        loop factor..limit .step:factor 'i -> 'multiples ++ i

    sum unique multiples
]
